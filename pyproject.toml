[project]
name = "skilling-analytics"
version = "0.1.0"
requires-python = ">=3.12,<3.13"

dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pandas",
    "numpy",
    "scikit-learn"
]

[tool.vercel]
entrypoint = "api:app"